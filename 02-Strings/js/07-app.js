const producto = "Monitor 27'";

console.log(producto.toUpperCase()); //Transforma todo el string en mayuscula.

const email = 'AARONVIGIL96@GMAIL.COM';
console.log(email.toLowerCase()); //Transforma todo el string en minuscula.

precio = 300;
console.log(precio)
console.log(precio.toString()); // Transforma el tipo de dato numero en string.