//Formas de declarar booleano. puede ser true o false.
const boolean1 = true;
const boolean2 = false; 
const boolean3 = "true"; //No sé puede declarar así.
const boolean4 = new Boolean(true); //Arroja un boolean true, pero su tipo de dato es un objeto.