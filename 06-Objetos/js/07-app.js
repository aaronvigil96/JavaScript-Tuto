const producto = {
    nombre: "Reloj",
    precio: 500,
    disponible: true
}

console.log(Object.keys(producto)); //Devuelve un array con las propiedades del objeto.
console.log(Object.values(producto)); //Devuelve un array con los valores del objeto.
console.log(Object.entries(producto)); //Devuelve arrays con los pares de propiedades y valor.